---
# Bundles in this catalog already use registry.redhat.io pullspecs.
# Kept so the FBC pipeline IDMS rewrite step has a file to consume.
apiVersion: config.openshift.io/v1
kind: ImageDigestMirrorSet
metadata:
  name: catalog-idms
spec:
  imageDigestMirrors:
  - mirrors:
    - quay.io/redhat-user-workloads/telco-5g-tenant/topology-aware-lifecycle-manager-bundle-4-22
    source: registry.redhat.io/openshift4/topology-aware-lifecycle-manager-operator-bundle
