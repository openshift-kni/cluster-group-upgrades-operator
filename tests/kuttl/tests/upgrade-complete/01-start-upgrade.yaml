apiVersion: kuttl.dev/v1beta1
kind: TestStep

commands:
  # Start the upgrade by enabling the UOCR.
  - command: oc patch cgu/cgu-upgrade-complete --patch '{"spec":{"enable":true}}' --type=merge
    ignoreFailure: false
