apiVersion: kuttl.dev/v1beta1
kind: TestStep

commands:
  - command: oc delete events --all
    namespaced: true
  - command: oc apply -f ../../../../deploy/upgrades/validation-failure-events/cgu-missing-clusters.yaml
    namespaced: true
